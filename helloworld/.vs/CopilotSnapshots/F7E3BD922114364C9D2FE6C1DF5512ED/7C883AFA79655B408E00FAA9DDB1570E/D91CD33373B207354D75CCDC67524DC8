#include<stdio.h>
int main()
{
	char* file = "C:\\Users\\noname37\\Desktop\\aaa.txt";
	printf("%s\n", file);
	FILE* fp = fopen(file, "r");
	if (fp == NULL)
	{
		perror("打开文件失败");
		return 1;
	}
	int c;
	while ((c = fgetc(fp)) != EOF)
	{
		printf("%c\n", c);
	}
	fclose(fp);

	return 0;
}